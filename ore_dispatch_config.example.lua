return {
    -- ============================================================
    -- GTNH Ore Dispatcher v0.3.0
    -- 专用“矿处成品网”架构
    -- ============================================================

    -- 【必须填写】
    -- 成品网二合一 ME Interface UUID。
    -- OC 只从这里读取请求器所指定的最终产物库存：
    -- Barium Dust / Neodymium Dust / Ruby / Diamond ...
    productMeAddress = nil,

    -- 【必须填写】
    -- 原矿缓存网二合一 ME Interface UUID。
    -- OC 从这里扫描 Barium Ore / Ruby Ore 等待处理原矿。
    cacheMeAddress = nil,

    -- ============================================================
    -- Storage Bus
    -- ============================================================

    -- 若 OC 只连接一个 me_storagebus，可留 nil 自动识别。
    -- 如果以后同一台 OC 连接多个存储总线，则必须固定 UUID。
    storageBusAddress = nil,

    -- 留 nil 时自动扫描 side 0~5。
    -- 你此前实机测试的那根 Storage Bus 为 side=2，
    -- 但换 Adapter/MFU 方位后不保证仍是 2。
    storageSide = nil,

    -- ============================================================
    -- 调度参数
    -- ============================================================

    -- 每轮重新读取：请求器目标、成品库存、原矿缓存。
    controlInterval = 3,

    -- 滞回控制：
    -- 未处理时低于 95% 才重新开启；
    -- 一旦开启，一直补到 100% 才停止。
    reopenRatio = 0.95,
    stopRatio = 1.00,

    -- 同时允许进入集成矿处的最大矿种数量。
    maxActive = 12,

    -- 只管理 Storage Bus 前 N 个过滤槽。
    -- nil = 使用该 Storage Bus 当前全部有效槽位。
    managedSlots = 16,

    -- ============================================================
    -- 安全
    -- ============================================================

    -- 第一次部署必须建议保持 true：
    -- 只计算和显示，不实际修改 Storage Bus。
    dryRun = true,

    -- ============================================================
    -- UI
    -- ============================================================

    enableUI = true,
    progressBarWidth = 18,

    -- ratio = 最缺的排最上面
    -- name  = 按名称排序
    uiSort = "ratio",

    -- ============================================================
    -- ME 请求器
    -- ============================================================

    -- 程序会自动扫描所有 level_maintainer。
    -- 这里表示“每个请求器读取多少个槽”，不是请求器数量。
    requesterSlots = 5,

    -- ============================================================
    -- 特殊矿物例外
    -- ============================================================

    -- 正常材料依靠 OreDictionary 自动匹配：
    -- dustBarium <-> oreBarium
    -- gemRuby    <-> oreRuby
    -- 少数异常材料才需要在此文件中添加覆盖。
    overrideFile = "/home/ore_dispatch_overrides.lua",
}
